{
  "name": "@stashbridge/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute stashbridge --local --file=migrations/0001_init.sql",
    "migrate:remote": "wrangler d1 migrations apply stashbridge --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
